= display_page_title ('Leaves List')
.container
  .content
    %table.list-table
      %thead
        %tr
          %th= label_name('leave.date')
          %th= label_name('leave.hours')
          %th= label_name('leave.reason')
        - @leaves.each do |leave|
          %tr
            %td= leave.date
            %td
              = leave.hours
              = 'hour(s)'
            %td= leave.reason
    .pagination
      = bootstrap_paginate(@leaves)
